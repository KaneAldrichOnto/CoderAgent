# Real config with secrets — never commit
CoderAgentConfig.yaml

# Actual prompts — never commit
prompt.md

# Agent logs
logs/

# Agent scratchpad (inter-iteration notes)
agent_scratchpad.md

# Rolling commit log — written by the agent loop, regenerated locally
commit_log.md

# Agent done signal
.agent_done

# Operator steering override
steering.md

# Internal commit logs
InternalLogs/

# gui_nav.py persistent server log + RDP keep-alive session id
gui_nav_server.log
session_id.txt

__pycache__/
*.pyc
